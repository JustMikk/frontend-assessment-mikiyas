# Orders

```bash
npm install
npm run dev
```

Open the URL Vite prints (usually `http://localhost:5173`).

See [`NOTES.md`](./NOTES.md) for design notes. Profiler evidence is in [`evidence/`](./evidence/).
